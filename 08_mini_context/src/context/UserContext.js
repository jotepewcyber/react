import React from "react";

const UserContext = React.createContext()

export default UserContext;
// After creating Context ,we create a provider which has access to all states ,which is all wrapped inside UserContext

